# 삼국지 TCG 게임 — 웹 목업

**삼국지 영웅모집**(Heroes Wanted 스타일 로그라이크 TCG)과, 특정 보스/지역에서 플레이되는
**히어로 블러드**(파이널판타지7 리버스의 Queen's Blood 스타일 보드 카드 게임)를 웹으로 만든 목업입니다.
삼국지 장수 테마로 리스킨되어 있습니다(장수별 스탯은 추후 세팅 예정).
구현 가능 여부와 게임성 확인을 목적으로 합니다.

> 최종 빌드는 Unity로 진행하되, 빠른 확인을 위해 먼저 웹으로 프로토타이핑했습니다.

## 구현 언어 (추천)

**HTML + CSS + Vanilla JavaScript** (프레임워크·빌드 도구 없음)

추천 이유:
- **즉시 실행 / 무설치** — 파일을 브라우저로 열기만 하면 PC·모바일 어디서나 동작하며, 빌드 단계가 없습니다.
- **모바일 지원** — 반응형 레이아웃과 탭(터치) 기반 조작으로 휴대폰에서 그대로 플레이됩니다.
- **의존성 0** — 외부 라이브러리/이미지 서버 없이 동작(아트는 이모지 + CSS 그라데이션 사용)하므로 오프라인에서도 안정적입니다.
- **이식성** — 카드 데이터와 규칙 로직이 순수 함수로 분리되어 있어 추후 Unity(C#)로 규칙을 옮기기 쉽습니다.

## 실행 방법

가장 간단한 방법은 `index.html`을 브라우저로 직접 여는 것입니다.

로컬 서버로 실행하려면(권장):

```bash
# 프로젝트 폴더에서
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

모바일에서 플레이하려면 PC와 같은 와이파이에 연결한 뒤 `http://<PC-IP>:8000` 으로 접속하세요.

## 화면 구성

- **첫 페이지(`index.html`)** — 난이도(상/중/하)를 고르고 두 게임 중 하나를 선택해 진입.
- **삼국지 영웅모집(`heroes.html`)** — 로그라이크 파티 전투.
- **히어로 블러드(`queensblood.html`)** — 3×5 보드 카드 배치 게임.

난이도는 첫 페이지에서 선택하며, 선택값은 URL 파라미터(`?diff=`)와 `localStorage`로 전달됩니다.

## 게임 1 — 삼국지 영웅모집 (로그라이크 파티 전투)

- **진행 경로(맵)**: 전투 / 정예 / 휴식 / 상점 / 보물 / 보스 노드를 갈래 중 선택하며 8층을 진행해 보스를 처치하면 클리어.
- **파티**: 시작 영웅 3명. 전투 보상으로 영웅 카드를 영입(최대 5명, 가득 차면 교체).
- **턴제 전투**:
  - 매 라운드 **에너지(⚡)** 를 받고, 영웅별로 **기본 공격(무료)** 또는 **스킬(에너지 소모)** 을 한 번씩 사용.
  - 적은 다음 행동(공격/전체공격)을 머리 위에 **예고(intent)** 합니다.
  - 영웅 탭 → 행동 선택 → 대상 선택 순서로 조작.
- **성장 요소**: 골드, 상점 구매(영웅/물약/무기강화), 휴식(회복/단련), 유물(전투 지속 효과).
- 16종의 영웅 카드(전사/수호/마법/사격/치유/암살 클래스)와 다양한 스킬(강타·광역·연속·치유·방어막·버프).

## 게임 2 — 히어로 블러드 (보드 카드 게임)

- **보드**: 가로 3줄 × 세로 5칸. 당신은 왼쪽 끝 열을, 상대는 오른쪽 끝 열을 폰 1레벨로 시작.
- **카드 배치**: 자신이 점령한 칸의 **폰 레벨(♟️)** 이 카드의 **요구 등급(핍)** 이상일 때만 배치 가능.
- **확장**: 카드를 놓으면 카드의 강화 패턴에 따라 빈 칸에 폰을 +1 부여(최대 3)하며 영역을 넓힘.
- **능력**: 일부 카드는 아군 파워 증가/적 파워 감소 등 지속 효과 보유.
- **점수**: 각 줄에서 양측 파워 합을 비교, 더 높은 쪽이 **두 합의 차이**만큼 득점. 3줄 합산 총점이 높은 쪽이 승리.
- **종료**: 양쪽이 연속 패스하거나 보드가 가득 차면 종료.
- **카드 32종**(요구 등급·파워·강화 패턴·능력이 모두 다른) 수록.
- AI 상대: 난이도에 따라 무작위(하) → 탐욕적 평가(중) → 1수 앞 예측(상)으로 강해집니다.

## 난이도(상/중/하)

| 게임 | 하(Easy) | 중(Normal) | 상(Hard) |
|------|----------|------------|----------|
| 삼국지 영웅모집 | 적 약화·골드↑·단순 AI | 기본 | 적 강화·골드↓·영리한 AI(최저 HP 우선 타격) |
| 히어로 블러드 | 무작위 AI | 탐욕적 AI | 1수 예측 AI·강한 덱 |

## 폴더 구조

```
index.html            첫 페이지(게임/난이도 선택)
heroes.html           삼국지 영웅모집
queensblood.html      히어로 블러드
css/
  common.css          공통 스타일
  home.css / heroes.css / queensblood.css
js/
  util.js             공통 헬퍼(난이도·셔플·토스트 등)
  heroes_data.js      영웅/적/유물/맵 데이터
  heroes.js           삼국지 영웅모집 엔진·UI
  qb_data.js          히어로 블러드 카드 32종·덱 데이터
  queensblood.js      히어로 블러드 엔진·UI
```

## 아트(이미지)

별도 이미지 서버 의존을 피하기 위해 **이모지 + CSS 그라데이션**으로 비주얼을 구성했습니다.
추후 카드/배경 이미지를 넣고 싶다면 각 카드 데이터의 `emoji` 필드를 이미지 경로로 교체하면 됩니다.

## 다음 단계(2차 이후 제안)

- 덱 빌딩/카드 컬렉션 화면, 카드 일러스트 적용
- 삼국지 영웅모집의 영구 성장(메타) 시스템, 더 많은 적/보스 패턴
- 히어로 블러드 카드 능력 추가(파괴 시 효과, 카드 이동/제거 등)
- 검증된 규칙 로직을 Unity(C#)로 포팅
